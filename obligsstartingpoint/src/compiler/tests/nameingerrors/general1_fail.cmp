program general1_fail
begin

proc main()
begin
    proc Tmp() : string
    begin
        return "Top of the world, ma!";
    end;

    var Tmp : int; // error, already a symbol named Tmp in current scope

    printstr( Tmp() );
end;

end;
