program general2_fail
begin
  procedure Proc( a : int )
  begin
    var a : int  (* error: param already named a. actually that should be ok. *)
    in
    printint( a )
  end;
  procedure main()
  begin 
    in
    Proc()
  end
end